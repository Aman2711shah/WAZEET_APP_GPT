# WAZEET App Environment Configuration
# Copy this file to .env and fill in your actual values
# DO NOT commit the .env file - it's in .gitignore

# OpenAI API Key (required for AI chatbot features)
# Get your key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# Firebase Configuration (usually auto-configured via firebase_options.dart)
# No additional environment variables needed for Firebase

# Optional: Stripe Keys (if payment features are enabled)
# STRIPE_PUBLISHABLE_KEY=your_stripe_publishable_key_here
# STRIPE_SECRET_KEY=your_stripe_secret_key_here

# Optional: Other API Keys
# Add any other API keys your app needs here
